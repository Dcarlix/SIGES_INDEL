@model SIGES_INDEL.Models.Registros.ActasAsignadas
@{
    SIGES_INDEL.Models.Estudiante estudiante = ViewBag.estudiante as SIGES_INDEL.Models.Estudiante ?? new SIGES_INDEL.Models.Estudiante();
    List<SIGES_INDEL.Models.Docente> xdocentes = ViewBag.docentes as List<SIGES_INDEL.Models.Docente>;
}
@{
    ViewData["Title"] = "Agregar Actas";
}
<form asp-action="Crear">
    <div class="container py-4">
        <h3 class="text-center mb-4 fw-bold">Agrega un Acta a @estudiante.NombreCompleto</h3>
        <div class="card mb-4 shadow-sm">
            <div class="card-body">
                <div class="row align-items-center">
                    <div class="col">
                        <div class="row g-3">
                            <div class="col-md-6">
                                <label asp-for="EstudianteId" class="control-label"></label>
                                <input type="hidden" asp-for="EstudianteId" value="@estudiante.Id" />
                                <input value="@estudiante.NombreCompleto" class="form-control" readonly />
                            </div>
                            <div class="col-md-6">
                                <label asp-for="FechaRegistro" class="control-label"></label>
                                <input asp-for="FechaRegistro" type="date" class="form-control" />
                                <span asp-validation-for="FechaRegistro" class="text-danger"></span>
                            </div>
                            <div class="col-md-6">
                                <label asp-for="DocenteId" class="control-label"></label>
                                <select asp-for="DocenteId" class="form-control">
                                    <option selected value="">Selecciona Docente</option>
                                    @foreach (var item in xdocentes)
                                    {
                                        <option value="@item.Id">@item.NombreCompleto</option>
                                    }
                                </select>
                                <span asp-validation-for="DocenteId" class="text-danger"></span>
                            </div>
                            <div class="col-md-12">
                                <label asp-for="Acta" class="control-label"></label>
                                <input asp-for="Acta" class="form-control" />
                                <span asp-validation-for="Acta" class="text-danger"></span>
                            </div>
                            <div class="col-md-12">
                                <label asp-for="Comentarios" class="control-label"></label>
                                <input asp-for="Comentarios" class="form-control" />
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        <div class="text-end">
            <button type="submit" class="btn btn-success">Agregar Acta</button>
            <a class="btn btn-secondary me-2" asp-controller="Expedientes" asp-action="Index" asp-route-id="@estudiante.Id">Cancelar</a>
        </div>
    </div>
</form>
@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}

